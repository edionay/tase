import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF257AA9);
const kSecondaryColor = Color(0xFF232228);
const kBackgroundColor = Color(0xFF0C0B10);

const kDefaultBorderRadius = BorderRadius.all(
  Radius.circular(10),
);

const kDefaultPadding = 16.0;

const tableHeaderStyle =
    TextStyle(color: kPrimaryColor, fontWeight: FontWeight.w900);
